<target name="{{ targetName }}" description="Run automated tests with PHPUnit">
    <exec executable="vendor/bin/phpunit" failonerror="true">
        <arg value="--colors=always" />
    </exec>
</target>
